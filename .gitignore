node_modules/
dist/
.env
.env.local
.claude/

# Generated build artifacts — rebuilt from data/activities.json each build.
# activities.geojson is the pre-rename name; keep ignoring it so a stale local
# copy is never committed or bundled into dist/.
app/public/tracks.json
app/public/activities.geojson
